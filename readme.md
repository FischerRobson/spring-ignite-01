# Spring Rest API

### Run app

```shell
mvn spring-boot:run
```

```shell
java -jar /target/ignite-spring-01-[version].jar
```

```mvn clean``` To clean /target

```mvn install``` To generate /target
